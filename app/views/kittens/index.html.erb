<h1>All Kittens</h1>

<%= link_to "New Kitten", new_kitten_path, class: "btn btn-primary" %>

<ul>
  <% @kittens.each do |kitten| %>
    <li>
      <%= link_to kitten.name, kitten_path(kitten) %>
      — Age: <%= kitten.age %>,
      Cuteness: <%= kitten.cuteness %>/10,
      Softness: <%= kitten.softness %>/10
      |
      <%= link_to "Edit", edit_kitten_path(kitten) %> |
      <%= link_to "Delete", kitten, data: { turbo_method: :delete, confirm: "Are you sure?" } %>
    </li>
  <% end %>
</ul>
